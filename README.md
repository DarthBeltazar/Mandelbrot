# Mandelbrot's set realtime visualizer
## Install instructions
### Macos
Download file for mac from releases and run `chmod +x path` with path to your app instead of word 'path' (you can drag file to terminal and path will be inserted automatically)
